<!DOCTYPE html>
<html lang="en">
  <head>
{{> head-first}}
    <title>Tutorials{{#if site.title}} | {{{site.title}}}{{/if}}</title>
    {{#if page.canonicalUrl}}
    <link rel="canonical" href="{{page.canonicalUrl}}">
    {{/if}}
{{> head-last}}
  </head>
  <body class="tutorials body tiles">
{{> header}}
{{> body-tutorials}}
{{> footer}}
  </body>
</html>
